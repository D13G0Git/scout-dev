// app.json parser STUB — Phase 2 will validate schema with zod.
import type { AppJson } from "@/lib/agent/types";

export function parseAppJson(_raw: string): AppJson {
  throw new Error("parseAppJson: not implemented (mockup)");
}
